# Example custom alert rule
groups:
  - name: custom_alerts
    rules:
      - alert: CustomHighCPU
        expr: rate(process_cpu_seconds_total[1m]) > 0.8
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "High CPU on custom service"
